apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mini-api-ingress
spec:
  rules:
  - host: mini.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mini-api
            port:
              number: 80
